# 腾讯云域名绑定说明：三条解析版

## 当前域名

- 中文域名：`耗子ai学习资源库.com`
- 根域名 Punycode：`xn--ai-422cl32chdr5tu07apk3ar1y.com`
- www 域名 Punycode：`www.xn--ai-422cl32chdr5tu07apk3ar1y.com`
- 当前网站托管：GitHub Pages
- 当前 GitHub Pages 地址：`https://haozi526.github.io/ai-resource-guide/`

## 推荐策略

腾讯云当前只能配置三条解析，所以使用 `www` 作为主站地址：

```text
https://www.耗子ai学习资源库.com
```

根域名：

```text
https://耗子ai学习资源库.com
```

用腾讯云 DNSPod 的显性 URL 转发跳转到 `www` 主站。

## 第一步：删除旧 A 记录

如果腾讯云 DNSPod 里已经添加过下面这些根域名 A 记录，请先删除：

```text
@  A  185.199.108.153
@  A  185.199.109.153
@  A  185.199.110.153
@  A  185.199.111.153
```

原因：GitHub Pages 根域名官方推荐 4 条 A 记录；你当前只有三条解析额度，所以不再使用根域名 A 记录方案。

## 第二步：腾讯云 DNSPod 添加解析

只需要配置下面 2 条，第三条可以留空：

| 主机记录 | 类型 | 记录值 |
|---|---|---|
| `www` | `CNAME` | `Haozi526.github.io` |
| `@` | `显性URL转发` | `https://www.xn--ai-422cl32chdr5tu07apk3ar1y.com` |

填写注意：

- `www` 的 CNAME 只填 `Haozi526.github.io`。
- 不要在 CNAME 里写仓库名。
- 不要在 CNAME 里写 `/ai-resource-guide`。
- 如果腾讯云当前套餐不支持“显性URL转发”，先只配置 `www` 的 CNAME，根域名暂时不启用。

## 第三步：GitHub Pages 绑定自定义域名

进入 GitHub 仓库：

1. 打开 `Haozi526/ai-resource-guide` 仓库。
2. 进入 `Settings`。
3. 左侧进入 `Pages`。
4. 在 `Custom domain` 输入：

```text
www.xn--ai-422cl32chdr5tu07apk3ar1y.com
```

5. 保存。
6. 等待 GitHub 检测 DNS。
7. DNS 检测通过后，勾选 `Enforce HTTPS`。

## 第四步：上传网站文件

发布文件里必须包含 `CNAME` 文件，内容为：

```text
www.xn--ai-422cl32chdr5tu07apk3ar1y.com
```

本项目已经在以下位置加入了 `CNAME`：

- 项目根目录
- `github-pages-public`
- `github-pages-upload-text`
- `github-pages-public.zip`

## 第五步：检查是否生效

在 PowerShell 里运行：

```powershell
Resolve-DnsName www.xn--ai-422cl32chdr5tu07apk3ar1y.com -Type CNAME
```

浏览器访问：

- `https://www.耗子ai学习资源库.com`
- `https://耗子ai学习资源库.com`

预期结果：

- `www.耗子ai学习资源库.com` 打开网站。
- `耗子ai学习资源库.com` 跳转到 `www.耗子ai学习资源库.com`。

DNS 解析可能需要几分钟到 24 小时。
